/*
 * Stub core_cmFunc.h — ChibiOS bundles CMSIS v5 (cmsis_gcc.h) which already
 * provides all Cortex-M intrinsic functions (__enable_irq, __get_BASEPRI etc.).
 * This empty file satisfies #include "core_cmFunc.h" from WICED SDK code
 * that was written for the older CMSIS v4 split headers.
 */
#pragma once
